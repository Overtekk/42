Since the cursus change since december 08 2025, the cursus is now divided.\
I've done so_long before it vanish. Also, push_swap was requiered to complete to go in the new python cursus.

You can check my old work in `old_cursus` and the new one in `new_cursus`.

**Circle 0 and 1** don't change since it was only the old one. **Circle 3** is only the new cursus so no problem with that.
